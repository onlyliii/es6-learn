/**
 * Created by pinli on 2/10/2017.
 */
// CommonJS模块
let {stat, exists, readfile} = require('fs');
//等同于
let _fs = require('fs');
let stat1 = _fs.stat;
let exists1 = _fs.exists;
let readfile1 = _fs.readfile;
console.log(stat === stat1);
console.log(exists === exists1);
console.log(readfile === readfile1);
